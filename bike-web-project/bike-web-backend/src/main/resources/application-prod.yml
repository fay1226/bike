#Server配置
server:
  port: 81
  servlet:
    context-path: /bk

#Spring数据源配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/bike
    name: root
    password: root

#Mybatis配置
mybatis:
  config-location: classpath*:mapper/*Mpper.xml
